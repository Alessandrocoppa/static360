import { RiduciClassificaPipe } from './riduci-classifica.pipe';

describe('RiduciClassificaPipe', () => {
  it('create an instance', () => {
    const pipe = new RiduciClassificaPipe();
    expect(pipe).toBeTruthy();
  });
});
